const books = [\n    {\n        title: "The Great Gatsby",\n        author: "F. Scott Fitzgerald",\n        image: "../images/book-covers/book1.jpg",\n        price: 10.99,\n        description: "A classic novel of the American dream."\n    },\n    {\n        title: "1984",\n        author: "George Orwell",\n        image: "../images/book-covers/book2.jpg",\n        price: 8.99,\n        description: "A dystopian novel criticizing totalitarianism."\n    },\n    // ... more book objects as needed\n];\n\nfunction loadFeaturedBooks() {\n    const bookGrid = document.getElementById("book-grid");\n    bookGrid.innerHTML = "";\n\n    books.forEach((book, index) => {\n        const bookCard = `\n            <div class="book-card">\n                <img src="${book.image}" alt="${book.title}">\n                <h3>${book.title}</h3>\n                <p>by ${book.author}</p>\n                <p>Price: $${book.price.toFixed(2)}</p>\n                <button onclick="addToCart(${index})">Add to Cart</button>\n            </div>\n        `;\n        bookGrid.innerHTML += bookCard;\n    });\n}\n\nfunction searchBooks() {\n    const query = document.getElementById("search").value.toLowerCase();\n    const filteredBooks = books.filter(book => book.title.toLowerCase().includes(query) || book.author.toLowerCase().includes(query));\n    const bookGrid = document.getElementById("book-grid");\n    bookGrid.innerHTML = "";\n\n    filteredBooks.forEach((book, index) => {\n        const bookCard = `\n            <div class="book-card">\n                <img src="${book.image}" alt="${book.title}">\n                <h3>${book.title}</h3>\n                <p>by ${book.author}</p>\n                <p>Price: $${book.price.toFixed(2)}</p>\n                <button onclick="addToCart(${index})">Add to Cart</button>\n            </div>\n        `;\n        bookGrid.innerHTML += bookCard;\n    });\n}\n\nfunction addToCart(index) {\n    // Cart functionality would go here\n    alert(\